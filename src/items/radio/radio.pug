mixin radio(id, title, name, checked)
    .radio(class!=attributes.class)
        input.radio__input(type="radio" id=id name=name checked=checked class!=attributes.field)
        label.radio__label(for=id class!=attributes.label)!= title
